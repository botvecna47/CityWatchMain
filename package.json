{
  "name": "citywatch-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json to manage both backend and frontend for CityWatch",
  "scripts": {
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.13",
    "concurrently": "^9.2.1"
  }
}
